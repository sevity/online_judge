/** @type {import('next').NextConfig} */
const nextConfig = {
    //webpackDevMiddleware: config => {
      // Disable HMR
      //config.hot = false;
      //return config;
    //},
  }
  
  module.exports = nextConfig
  